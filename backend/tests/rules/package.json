{
  "name": "shinatal-firestore-rules-tests",
  "private": true,
  "type": "module",
  "description": "Suite automatizada de testes para backend/firestore.rules, rodando contra o Firebase Emulator (nunca contra produção).",
  "scripts": {
    "test": "cd ../../.. && firebase emulators:exec --project demo-shinatal --only firestore \"node --test backend/tests/rules/firestore-rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.13.0"
  }
}
